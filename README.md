# StudyStack V2 改修ミッション

StudyStack V1を完成版として凍結し、V2では

「既存システムに追加要件が発生した」

という想定で、保守・改修を行う。

## 必須要件

- Study投稿時に、あらかじめ定義されたタグを選択できる
- タグによるStudyの絞り込みができる
- 週間目標を設定できる

## 拡張要件

- 週間目標を約7週間分表示し、期限管理を行う
- 期限数日前の通知機能

## 改修方針

追加要件確認  
→ 既存コードの影響範囲調査  
→ 改修方針決定  
→ 小さく実装  
→ 追加テスト  
→ 既存テストによる回帰確認

一度に大きく変更するのではなく、小さな単位で実装と確認を繰り返し、既存機能への影響を確認しながら改修を進める。


# タグ機能 要件

## 基本仕様

- タグは運営側があらかじめ定義する
- 一般ユーザーによるタグの追加・編集・削除は行わない
- 1つのStudyに設定できるタグは最大2個
- タグを設定しないStudyも登録可能
- 同一Studyに同じタグを重複して設定することはできない
- 同一名のタグは複数作成できない
- StudyとTagは多対多（Many-to-Many）の関係とする

## 新規登録

- Study投稿時に定義済みタグから選択できる
- 最大2個まで選択可能
- タグを選択せずにStudyを登録することも可能
- 3個以上選択した場合はバリデーションエラーとする

## 更新

- Studyに設定済みのタグを表示する
- タグの変更・追加・解除ができる
- 更新後も0〜2個の制約を維持する
- 3個以上選択した場合は更新せず、既存データを維持する
- 同じタグの重複設定は許可しない

## 一覧表示

- DashboardのStudy一覧に設定済みタグを表示する

## タグ絞り込み

- 定義済みタグを利用してStudy一覧を絞り込めるようにする

## UI方針

- 現在は複数選択可能なタグ選択UIを使用する
- 将来的にJavaScriptを利用し、選択したタグをチップ形式で表示する
- チップから選択済みタグを解除できるようにする
- 同一タグを複数回選択できないようUI側でも制御する
- UI装飾は機能実装・動作確認後に行う


# タグ機能 影響範囲調査

## models.py

影響あり。

V1のStudyモデルは以下の情報を保持していた。

- user
- content
- created_at

タグ情報を保持する仕組みが存在しなかったため、新しくTagモデルを追加し、Studyとの関連を定義する必要がある。

1つのStudyに複数のTagを設定でき、同じTagを複数のStudyで利用するため、StudyとTagは多対多の関係となる。

そのため、Studyモデルに `ManyToManyField` を追加する。

また、同一名のタグが複数作成されることを防ぐため、Tagの `name` には `unique=True` を設定する。


## forms.py

影響あり。

V1のStudyFormはcontentのみを入力項目として扱っていたため、タグを選択できるようフォームを拡張する。

タグは未選択を許可し、以下の条件でバリデーションを行う。

- タグ0個：許可
- タグ1個：許可
- タグ2個：許可
- タグ3個以上：不許可

タグ最大2件の制約は `StudyForm` のバリデーションで行う。


## views.py

影響あり。

新規登録では以下の流れでStudyを保存する。

- StudyFormを生成
- `form.is_valid()` で検証
- `form.save(commit=False)` でStudyを生成
- ログインユーザーを設定
- Study本体を保存
- `form.save_m2m()` でTagとの多対多の関連を保存

ManyToManyの関連はStudy本体の保存とは別に保存する必要があるため、`form.save_m2m()` を追加する。

Updateでは既存Studyを `instance` としてStudyFormへ渡し、現在のタグを保持した状態から変更・追加・解除を行う。


## templates

影響あり。

### dashboard.html

以下を追加・変更する。

- タグ選択UI
- Study一覧への設定済みタグ表示
- タグバリデーションエラー表示

タグ一覧はStudyごとに `study.tags.all` を利用して表示する。

### update.html

以下を扱えるよう変更する。

- 現在設定されているタグの表示
- タグの追加
- タグの変更
- タグの解除
- タグバリデーションエラー表示


## tests.py

影響あり。

V1で作成した19件のテストを維持したまま、タグ機能に対するテストを追加する。

現在は29件のテストを実装し、すべてGREENである。

追加した主な確認項目は以下。

### Form

- タグ2個ならバリデーションを通過する
- タグ3個ならバリデーションエラーになる

### Model

- Studyに複数のTagを関連付けできる
- 同じTagを同じStudyへ複数回追加しても関連が重複しない
- 同じ名前のTagを複数作成できない

### View

- Dashboardからタグ付きStudyを作成できる
- Dashboardに設定済みタグが表示される
- Studyのタグを更新できる
- タグの追加・解除が正しく反映される
- 3タグで新規登録した場合、Studyが作成されない
- 3タグで更新した場合、既存Studyが変更されない
- バリデーションエラーが画面に表示される

既存テストも含めて全件を実行し、タグ機能追加後も既存機能が壊れていないことを回帰確認する。


# 実装中に確認した仕様・設計上の注意点

## タグ最大2件の制約について

タグ最大2件の制約は `StudyForm` で実装している。

そのため、DashboardやUpdate画面などStudyFormを経由したユーザー操作では3件以上のタグを設定できない。

一方で、Django Shellなどから直接、

`study.tags.add(tag)`

を実行した場合はStudyFormを経由しないため、3件以上のTagをStudyへ関連付けることが可能であることを確認した。

例：

- Python
- Django

が設定済みのStudyに対して、ShellからAWSを直接追加すると、

- Python
- Django
- AWS

の3件を関連付けることができる。

V2では一般ユーザーによる操作をStudyForm経由に限定するため、タグ件数の制約はForm層で管理する方針とする。

Model・DB層まで含めて「どの経路からでも必ず最大2件」とする厳密な制約については、V2では実装対象外とする。


## 同一Tagとの関連について

同じTagを同じStudyへ複数回 `add()` した場合でも、StudyとTagの関連は重複して作成されないことをShellおよびModelテストで確認した。

例：

StudyとPythonがすでに関連付いている状態で、

`study.tags.add(python_tag)`

を再度実行しても、Pythonとの関連は1件のままとなる。


## 同名タグについて

Tagの `name` に `unique=True` を設定し、完全一致する同名タグを複数作成できないようにした。

例：

- `Python`
- `Python`

は重複登録不可。

ただし、

- `Python`
- `python`
- `PyThOn`

など、大文字・小文字や表記揺れを同一タグとして扱うかどうかについてはV2では扱わない。

この問題は、ユーザー自身がタグを作成できる次回作の技術ブログで、タグ名の正規化を含めて要件から検討する。


# 現在の実装状況

## 完了

- Tagモデル追加
- StudyとTagのManyToMany関連
- Study投稿時のタグ選択
- タグ最大2件のFormバリデーション
- タグなしStudyの登録
- Dashboardでのタグ表示
- Updateでのタグ変更・追加・解除
- 同一Study・Tag間の関連重複防止確認
- 同名Tagの重複登録防止
- タグ機能に対するModel / Form / Viewテスト
- 既存テストを含む29件の回帰テスト

## 未実装

- タグによるStudy絞り込み
- JavaScriptによるタグのチップ表示・解除UI
- 週間目標機能
- 週間目標の履歴表示
- 期限管理・通知機能


# 今後の改修予定

1. タグによるStudy一覧の絞り込み
2. タグ絞り込み機能の追加テスト
3. 既存Viewテストのresponse確認を含む最終点検
4. タグ機能全体の回帰テスト
5. 週間目標機能の実装
6. JavaScriptによるタグ選択UIの改善
7. 拡張要件の検討・実装